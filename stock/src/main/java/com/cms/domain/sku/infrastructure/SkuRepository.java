package com.cms.domain.sku.infrastructure;

public interface SkuRepository {
//    @Lock(value = LockModeType.OPTIMISTIC)
//    @Query("select s from Stock s where s.id = :id")
//    Optional<Stock> findByIdForUpdate(@Param("id") Long id);
}
